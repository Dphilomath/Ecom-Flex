package com.dm.ecommerce.config;

/**
 * Enum representing the available authentication modes in the application.
 */
public enum AuthMode {
    STATELESS, // JWT-based authentication
    STATEFUL   // Session-based authentication
} 